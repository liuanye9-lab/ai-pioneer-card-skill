# 企业级 Skill 框架核查（ENTERPRISE-AUDIT）

依据「企业级 Skill 怎么写」框架，对 AI先锋大赛智能飞书卡片 Skill 逐维核查。
图例：✅ 已满足 · 🟡 部分满足 · ❌ 缺失（本次补齐）。

---

## 1. 核心目标

| 目标 | 状态 | 依据 |
|---|---|---|
| 准确命中 | ✅ | Intent Router（confidence<0.65 fallback announcement），Render/Image Router 三段一致 |
| 稳定执行 | ✅ | 确定性规则管线（非自由 LLM），Zod/Schema 校验，56+ 单测 |
| 低成本运行 | ✅ | 纯规则、无外部 LLM 调用；离线可运行；性能目标 P95<20s |
| 降低幻觉 | ✅ | Source of Truth + Fact Locker + Fact QA（活动名/URL/Emoji/Deadline hard-fail） |
| 易维护 | ✅ | 模块边界清晰（11 目录），类型集中 `core/types.ts`，模板只描述布局策略 |

结论：核心目标全部满足。

---

## 2. 分层原则

| 原则 | 状态 | 说明 |
|---|---|---|
| 每层信息服务对应场景 | ✅ | 触发/执行/兜底/参考四层各司其职（见 SKILL.md v2） |
| 触发信息与执行信息分离 | 🟡→✅ | 原 SKILL 混写；v2 已拆分「触发层」与「执行层」 |
| 异常处理独立 | ❌→✅ | 新增独立「兜底层」+ `src/core/preflight.ts` + `Fallback/Preflight` 输出段 |
| 参考资料按需加载 | ✅ | 参考层（Template/Schema/术语/业务规则/长文档）标注「按需加载，不触发就不加载」 |
| 防止信息浪费 | ✅ | 语义去重 + 密度控制 + Progressive Disclosure |

结论：补齐异常处理独立 + 触发/执行分离后满足。

---

## 3. 三层抽象

| 层 | 对应 | 状态 |
|---|---|---|
| 索引层：命中 | 触发层（Name/Description/适用场景/边界/触发条件） | ✅（SKILL.md v2 顶部，精简可被 Agent 快速匹配） |
| 内容层：执行 | 执行层（输入/步骤/参数/工具/输出/质量） | ✅ |
| 附件层：按需加载 | 参考层（PRD/SPEC/DESIGN/schemas/brands/templates） | ✅ |

结论：满足。索引层文案已「短、准、边界清楚」。

---

## 4. 四层写法

### A. 触发层
| 项 | 状态 |
|---|---|
| Name / Description | ✅ |
| 适用场景 / 不适用场景 | 🟡→✅（原缺「不适用场景」，v2 补齐） |
| 相邻 Skill 边界 | ❌→✅（v2 明确与「日历/多维表/文档写作/图片生成」Skill 的边界） |
| 触发条件 | ✅ |

### B. 执行层
| 项 | 状态 |
|---|---|
| 输入要求 / 执行步骤 / 关键参数 / 工具调用逻辑 / 输出格式 / 质量标准 | ✅（22 步流水线 + 输出 bundle + 六维评分） |

### C. 兜底层（本次重点补齐）
| 项 | 原状态 | 补齐方式 |
|---|---|---|
| 缺参数追问 | ❌ | `preflight.ts` 检测缺关键事实/发送目标 → 生成 `clarifications[]` |
| 输入不足追问 | ❌ | 文案过短/无可提取事实 → 阻断并追问 |
| 工具失败降级 | 🟡 | 图片失败链 image_led→image_assisted→text_first（已有）+ 显式记录 |
| 依赖失败处理 | ✅ | 飞书凭证缺失→离线 Generated；品牌未缓存→默认视觉 |
| 低置信标注 | ❌ | intent.confidence<0.65 + uncertain_information → `low_confidence[]` 输出 |
| 越界拒绝 / 转其他 Skill | ❌ | 非卡片类输入（纯问答/长文写作/纯图片）→ `boundary` 拒绝 + 建议目标 Skill |
| 风险确认 | ❌ | `--send` / 真实发布 → 需显式确认（`requireSendConfirmation`） |

### D. 参考层
| 项 | 状态 |
|---|---|
| Template / Schema / 示例 / 术语表 / 业务规则 / 行业规范 / 长文档 | ✅（templates/、schemas/、examples/、prompts/、PRD/SPEC/DESIGN） |
| 原则：只挂载、按需加载、能不触发就不触发 | ✅（SKILL.md v2 参考层显式声明） |

---

## 5. 固定顺序

`触发 → 执行 → 兜底 → 参考`，对应链路 `发现 → 完成 → 处理异常 → 补充资料`。
状态：❌→✅ SKILL.md v2 按此顺序重排；管线在「执行」前置 preflight（兜底前移做守门），符合「先命中、再执行、异常可控」。

---

## 6. 写法规则

| 规则 | 状态 |
|---|---|
| 标准骨架 | ✅ 四层骨架 |
| 业务裁剪 | ✅ 卡片类型化差异 |
| 每个 Skill 经过四层检查 | ✅ 本审计即四层检查 |
| 不一定每层都写满 | ✅ 轻任务（如纯 deadline）不加载图片/导航参考 |
| 轻任务轻写 / 重业务写全 / 高风险写细 | ✅ text_first 轻；image_led_navigation 全；发送/回调高风险写细 |
| 强依赖补齐异常与参考 | ✅ 飞书发送/回调补齐兜底与参考 |

---

## 7. 适配维度

| 维度 | 本 Skill 定位 |
|---|---|
| 任务复杂度 | 中（多阶段编译，但确定性） |
| 业务风险 | 中高（对外群发、事实错误代价高）→ 兜底写细 |
| 工具依赖 | 中（飞书 OpenAPI；图片可选） |
| 输出规范性 | 高（Card JSON 2.0 + Schema 校验） |
| 专业知识密度 | 高（飞书卡片 + 信息架构 + 品牌） |
| 多 Skill 环境 | 是（需边界声明，见触发层 A） |

结论：属于「重业务 + 高风险 + 高规范」象限，四层写全、兜底写细——已对齐。

---

## 8. 检查清单

| 检查 | 结论 |
|---|---|
| 能否被正确发现 | ✅ 触发层 Name/Description/场景/边界清晰 |
| 能否稳定执行 | ✅ 58 单测 + 7 示例 bundle 全 pass |
| 异常是否可控 | ✅（补齐 preflight 兜底后）缺参追问/越界拒绝/低置信标注/风险确认 |
| 参考是否按需加载 | ✅ 参考层显式「不触发不加载」 |
| 边界是否清晰 | ✅ 相邻 Skill 边界已声明 |
| 成本是否可接受 | ✅ 纯规则、离线、无 LLM 调用成本 |

---

## 本次补齐清单（差距 → 动作）
1. **兜底层缺失** → 新增 `src/core/preflight.ts`（缺参追问 / 输入不足 / 越界拒绝 / 低置信标注 / 风险确认），集成进 `compile()`，输出 `preflight` 段与 `preflight.json`。
2. **SKILL.md 结构** → 重写为四层骨架（触发→执行→兜底→参考），补「不适用场景 / 相邻 Skill 边界」。
3. **Doubao 智能体封装** → 新增 `agent/`（manifest + system prompt + tool schema + adapter），将本 Skill 作为智能体核心工具嵌入。
